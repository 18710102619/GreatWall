include_directories(${PROJECT_SOURCE_DIR}/libGWall)
set(APP_SRC main.c)
# set(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
add_executable(Project1 ${APP_SRC})
target_link_libraries(Project1 libGWall)